language: go

go:
  - 1.6

sudo: required

services:
  - docker
  
script:
  - travis_wait 60 make quick-release
  - travis_wait 30 make test
  
# blocklist
branches:
  except:
  - legacy
  - experimental

# safelist
branches:
  only:
#  - master
#  - stable
  - cuericlee-patch-1
